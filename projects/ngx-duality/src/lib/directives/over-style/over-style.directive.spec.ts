import { OverStyleDirective } from './over-style.directive';

describe('OverStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new OverStyleDirective();
    expect(directive).toBeTruthy();
  });
});
