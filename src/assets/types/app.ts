export interface Sidebar {
  menu: string;
  sub_menu: Array<string>;
  link: Array<string>;
  icon: string;
  // sub_icon: Array<string>
}

export interface Dashboard {
  title: string;
  description: string;
}
